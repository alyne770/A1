@model LivroDigital.Models.Livro

@{
    ViewBag.Title = "Deletar Livro";
}

<div class="container mt-5">
    <div class="card">
        <div class="card-header bg-danger text-white">
            <h4 class="mb-0">@ViewBag.Title</h4>
        </div>
        <div class="card-body">
            <h3>Tem certeza de que deseja deletar este livro?</h3>
            <div class="mb-4">
                <h4>Detalhes do Livro</h4>
                <hr />
                <dl class="row">
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Editora.Nome)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Editora.Nome)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Titulo)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Titulo)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.AnoPublicacao)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.AnoPublicacao)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.ISBN)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.ISBN)</dd>
                </dl>
            </div>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="d-flex justify-content-end mt-4">
                    <button type="submit" class="btn btn-danger me-2">
                        <i class="bi bi-trash"></i> Deletar
                    </button>
                    @Html.ActionLink("Voltar para a Lista", "Index", null, new { @class = "btn btn-secondary" })
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
